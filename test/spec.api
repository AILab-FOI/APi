channel c .
agent bla_stdin_stdout :
	c -> self
agent bla_file_stdout :
	self -> c
start a+ 
start (a b) | c 
start (a b) & c 
environment : 
	input => json({ 'val1': ?x }) 
	output <= json({ 'val2': y }) 
